[build]
builder = "NIXPACKS"

# 👇 FORCE Railway to include /data folder in deployment
[[build.assets]]
source = "data"
destination = "/app/data"

[deploy]
startCommand = "streamlit run app.py --server.port=$PORT --server.address=0.0.0.0"
