[build]
builder = "NIXPACKS"

# 👇 THIS correctly tells Nixpacks to include the /data folder in the image
[[build.assets]]
source = "data"
destination = "/app/data"

[deploy]
startCommand = "streamlit run app.py --server.port=$PORT --server.address=0.0.0.0"
